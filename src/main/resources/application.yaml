server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vkad_db
    username: root
    password: MyPass!123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true

  application:
    name: auth-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

logging:
  level:
    org:
      springframework: INFO

